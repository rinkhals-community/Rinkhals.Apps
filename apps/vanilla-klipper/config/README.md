This directory is there to avoid:

Moonraker warning
Supplied path (/useremain/home/rinkhals/apps/vanilla-klipper/config) for (config_examples) is invalid. Make sure
that the path exists and is not the file system root.